iwr는 PowerShell의 별칭으로 Invoke-WebRequest 를 뜻해요.

웹에 HTTP 요청을 보내는 명령입니다. (비슷한 친구: irm= Invoke-RestMethod)

iwr는 원시 응답을 줍니다(헤더/본문/상태코드 다 포함).

irm는 JSON이면 자동으로 파싱해서 PowerShell 객체로 돌려줘서 편함.

GET은 보통 “조회/상태 확인”, POST는 “서버에 데이터 보내서 작업 수행”.

왜 한 터미널이 “멈춘 것처럼” 보이냐?

서버가 실행 중이라 그 터미널은 서버 로그 창이 돼.
API 테스트는 다른 터미널을 하나 더 열어서 요청을 보내면 돼

# 헬스체크
iwr http://localhost:8000/healthz

서버 프로세스가 살아 있고, HTTP로 응답 가능한지를 가장 가볍게 확인해.
보통 응답은 200 OK와 간단한 JSON: {"ok": true} 같은 형태.

# 문제 요청 예시
$body = @{ domain = "arith"; diff = 1 } | ConvertTo-Json
iwr "http://localhost:8000/v1/training/next" -Method POST -ContentType "application/json" -Body $body


용어 간단 정리

Flask: 파이썬 경량 웹 프레임워크(라우팅/미들웨어/응답)

REST API: HTTP(URI/메서드)로 자원에 접근하는 표준 스타일

JWT: 클라이언트가 들고 다니는 서명된 토큰(서버 무상태 인증)

CORS: 다른 출처에서 브라우저 호출 허용 설정(개발은 *, 운영은 제한)

SQLAlchemy: 파이썬 ORM/쿼리 도구(모델 ↔ 테이블 매핑)

Alembic: DB 스키마 버전 관리(마이그레이션)

SQLite/MySQL: 내장형 DB / 서버형 DB

Redis: 메모리 캐시/큐/세션 저장소

.venv: 파이썬 가상환경(프로젝트별 패키지 분리)

Docker: 실행 환경(의존성/OS)까지 묶은 컨테이너

docker-compose: 여러 컨테이너를 한 번에 정의/실행

(선택) WebSocket: 서버↔클라 실시간 양방향 통신(추후 PvP용)

ORM (Object-Relational Mapping) : 객체 지향 프로그래밍의 객체와 관계형 데이터베이스의 테이블을 자동으로 변환해주는 기술입니다


**11/21 서버 기능 추가**

CORS(Cross-Origin Resource Sharing) : 다른 도메인에서 Flask API에 접근할 수 있도록 허용하는 기능

JWT 데코레이터 통일 : 여러 파일에서 중복되는 “사용자 인증 검사 코드”를 하나의 공용 데코레이터(require_user) 로 통합해서 모든 API가 똑같이 인증 절차를 거치도록 관리

Rate Limit” (요청 제한) : 서버 보안·안정성 쪽의 선택적 기능 - “같은 사용자가 짧은 시간에 너무 많은 요청을 보내지 못하게 막는 기능”

구분                  	수정/추가 내용	                             결과

.env	             환경변수 관리 시스템 도입	            설정 외부화 및 보안 향상
config.py	         dotenv 적용	                     .env 값 로드 및 기본값 설정
app.py	             구조 통합 및 CORS, SocketIO 추가      서버 일원화 및 실시간 대응 준비
auth.py	             JWT 인증 강화	                      dev / prod 모드 전환 가능
training.py          MVP 문제 API 완성	                  클라이언트 테스트 가능
Alembic	DB           마이그레이션 준비	                    DB 자동 버전 관리
SocketIO	         실시간 통신 이벤트 구현	            아레나 대전 기반 구축 완료